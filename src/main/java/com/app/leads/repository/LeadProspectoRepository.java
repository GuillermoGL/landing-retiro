package com.app.leads.repository;

import com.app.leads.entity.LeadProspecto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LeadProspectoRepository extends JpaRepository<LeadProspecto, Long>{

}
